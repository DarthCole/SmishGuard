# ── Build outputs ──
*.iml
.gradle/
build/
/captures
.externalNativeBuild
.cxx

# ── Local configuration (machine-specific) ──
local.properties

# ── IDE files ──
.idea/
*.swp
*~

# ── OS files ──
.DS_Store
Thumbs.db

# ── Signing keys (SECURITY: never commit keystores) ──
*.jks
*.keystore
signing.properties
