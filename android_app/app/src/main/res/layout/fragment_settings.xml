<!--
  fragment_settings.xml — Settings Screen Layout
-->
<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <!--
          LinearLayout arranges children in a single row (horizontal)
          or column (vertical). Simpler than ConstraintLayout but less flexible.
        -->

        <!-- Privacy info section -->
        <TextView
            android:id="@+id/text_privacy_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:lineSpacingExtra="4dp"
            android:textColor="@color/text_primary"
            android:padding="16dp"
            android:background="@color/safe_background" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginVertical="16dp"
            android:background="@color/text_secondary" />
        <!-- Simple horizontal divider line -->

        <!-- Clear history button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_clear_history"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Clear Analysis History"
            app:icon="@android:drawable/ic_menu_delete"
            xmlns:app="http://schemas.android.com/apk/res-auto" />

        <!-- Status text after clearing -->
        <TextView
            android:id="@+id/text_clear_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/primary"
            android:visibility="gone" />

    </LinearLayout>

</ScrollView>
